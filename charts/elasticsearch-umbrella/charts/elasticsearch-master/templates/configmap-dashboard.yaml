{{- if .Values.prometheus.enabled -}}
apiVersion: v1
data:
{{ (.Files.Glob "dashboards/*.json").AsConfig | indent 2 }} 
kind: ConfigMap
metadata:
  namespace: monitoring
  annotations:
  labels:
    grafana_dashboard: {{ include "master.fullname" . }}
    {{- include "master.labels" . | nindent 4 }}
  name: {{ include "master.fullname" . }}-dashboard
{{- end }}
